spring:
  application:
    name: otter

  # 数据库配置
  datasource:
    url: jdbc:postgresql://117.72.110.100:5432/otter
    username: admin
    password: post123ADMIN
    driver-class-name: org.postgresql.Driver

  # JPA配置
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  # Redis配置
  data:
    redis:
      host: 117.72.110.100
      port: 6379
      database: 4

  # 邮件服务配置
  mail:
    host: smtp.163.com
    port: 465
    username: ${MAIL_USERNAME:heyqings@163.com}
    password: ${MAIL_PASSWORD:HBhmtT8F3CZMNN4F}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  # Thymeleaf配置
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
    encoding: UTF-8
    mode: HTML

# JWT配置
jwt:
  secret: ${JWT_SECRET:2FsU#GykX19Zb3=u7wj$1+wG8%LpOaY3}
  expiration: 86400000 # 24小时

# Web3配置
web3:
  provider: http://localhost:8545
  network-id: 1

# IPFS配置
ipfs:
  host: localhost
  port: 5001

# AI服务配置
ai:
  url: http://localhost:8000
  api-key: your-api-key-here

# Swagger配置
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true

# 日志配置
logging:
  level:
    root: INFO
    top.heyqing.otter: DEBUG
    org.springframework.security: INFO
    org.hibernate.SQL: DEBUG